## Atomic Labs clone :rocket:

Para correr el proyecto localmente:

- Clonar el repo

```sh
$ git clone https://github.com/mariana-gaminho/atomic-clone.git
$ cd atomic-clone
```

- Ejecutar los siguientes comandos:

```sh
$ yarn && yarn start
```
